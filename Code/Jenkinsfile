pipeline {
    agent any
    stages {
        stage('🔍 Debug') {
            steps {
                echo "🔍 Debug completo..."
                checkout scm
                sh '''
                    echo "=== ESTRUTURA COMPLETA ==="
                    pwd
                    ls -la
                    echo "=== PASTA CODE ==="
                    ls -la Code/
                    echo "=== BACKEND ==="
                    ls -la Code/backend/ 2>/dev/null || echo "Backend não encontrado"
                    echo "=== FRONTEND ==="
                    ls -la Code/frontend/ 2>/dev/null || echo "Frontend não encontrado"
                    echo "=== DOCKER ==="
                    docker --version || echo "Docker não disponível"
                '''
            }
        }
    }
}